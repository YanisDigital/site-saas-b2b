# Лендинг beta-запуску — специфікація

Односторінковий сайт для публічного beta-тестування SaaS-продукту:
**система персонального та корпоративного моніторингу професійної інформації.**

Документ самодостатній: містить структуру блоків, усі фінальні тексти,
заголовки, CTA, мікрокопі форми, візуальні токени та інструкції із заміни
плейсхолдерів. Готовий до передачі в розробку без окремого етапу
опрацювання позиціонування.

Супровідний файл: `index.html` — готовий статичний лендинг (вбудовані CSS/JS).

---

## 0. Базові рішення

| Параметр | Значення |
|---|---|
| Назва продукту | плейсхолдер `[Назва]` — замінюється в одному місці (див. розділ 8) |
| Мова | тільки українська |
| Формат | один статичний `index.html`, без збірки, без CMS |
| Форма заявки | розмітка + JS-заглушка; коментар-маркер, куди підключити Formspree / Google Forms / власний endpoint |
| Пріоритет | зрозумілість → презентація продукту → довіра → бажання спробувати → заявка |

**Чого на сайті немає (обмеження брифу):** вигаданих функцій, детального
roadmap, майбутніх інтеграцій, внутрішньої архітектури, механіки
фільтрації, цін, перевантаження текстом.

---

## 1. Місія та позиціонування

### Місія (основна, для сайту й подальшої комунікації)

> Обсяг інформації давно перевищив людську здатність її опрацьовувати.
> Ми будуємо систему, яка відстежує інформаційний потік замість вас
> і залишає лише те, що справді стосується ваших цілей —
> для приватної людини, спеціаліста чи компанії.

### Місія одним реченням (футер, соцмережі, презентації)

> Потрібна інформація має знаходити вас сама — а не навпаки.

### Позиціонування (внутрішнє, для узгодженості текстів)

`[Назва]` — це SaaS-система персонального та корпоративного моніторингу
професійної інформації. **Не** дайджест, **не** Telegram-бот, **не**
новинний канал, **не** інфопродукт.

---

## 2. Візуальна концепція — «Instrument»

Метафора: **точний вимірювальний прилад**, а не «лендинг». Продукт витягує
сигнал із шуму — це закладено в кожну деталь оформлення. Світла тема,
один акцентний колір, hairline-лінійки як у бухгалтерському реєстрі,
моноширинний шрифт як наскрізний елемент системи (мітки, номери, схеми,
шлях у формі). Багато повітря, нуль декору «інфобізнесу».

Ключові відчуття: технологічність, точність, спокій, масштабованість.

### Кольорові токени

| Токен | HEX | Застосування |
|---|---|---|
| `--bg` | `#FBFBF9` | тло сторінки (тепле майже-біле) |
| `--bg-tint` | `#F4F4F0` | ховер моно-навігації, сегментів |
| `--surface` | `#FFFFFF` | картки, панелі, поля форми, візуал |
| `--ink` | `#10131A` | текст, заголовки; тло темних блоків (теза, статус-бар, футер, анонс) |
| `--ink-mid` | `#3A3F4A` | абзаци-ліди |
| `--ink-soft` | `#5D626D` | другорядний текст, моно-підписи |
| `--line` | `#E4E3DE` | hairline-роздільники між секціями та в сітках |
| `--line-strong` | `#D2D1CA` | рамки полів форми, крапки в рамці скриншотів |
| `--accent` | `#3B44F6` | кнопки, номери, апертура фільтра, акцентні бари сигналу |
| `--accent-press` | `#2A32D6` | ховер/натиск первинної кнопки |
| `--accent-soft` | `#ECEDFF` | (резерв) світле тло акцентних плям |
| `--footer-bg` / `--footer-ink` / `--footer-soft` | `#10131A` / `#F2F2EE` / `#8B909C` | футер і темні блоки |

### Типографіка

- **Текст і заголовки:** IBM Plex Sans (400 / 500 / 600), **self-hosted**, `font-display: swap`. Обрано замість Inter: інженерний характер, менш «AI-шаблонний».
- **Уся систематика:** IBM Plex Mono (400 / 500 / 600) — eyebrow з номерами секцій, навігація, мітки візуалу, номери кроків, підписи скриншотів, «шлях» у формі, футер-мета. Це навмисна наскрізна ознака стилю.
- H1 — `clamp(2.7rem, 6.6vw, 4.4rem)`, вага 600, `letter-spacing: -0.04em`, `line-height: 1.02`; частина фрази — акцентним кольором (`<em>`).
- H2 — `clamp(1.85rem, 3.4vw, 2.7rem)`, вага 600, `letter-spacing: -0.03em`.
- Body — 16px моб. → 17px десктоп, `line-height: 1.65`.
- Eyebrow — моно 11px, `letter-spacing: .16em`, uppercase, формат `NN · Назва`, завершується короткою hairline-лінією.

### Сітка та ритм

- Контейнер: `max-width: 1140px`, бічні відступи 20px (моб.) / 24px.
- Кожна секція відкривається `border-top: 1px solid --line` — суцільний «реєстровий» ритм.
- Вертикальні відступи секцій: 56px (моб.) → 88px (десктоп).
- Радіуси: 8px (поля, дрібні картки), 12px (сітки), 18px (великі панелі), 999px (кнопки).
- **Тіней немає.** Глибина — лише через hairline-рамки, темні блоки та зерно.
- Плівкове зерно (SVG `feTurbulence`) `position: fixed`, `opacity: .028` поверх усієї сторінки.
- Індикатор прокрутки — 2px акцентна смуга зверху (`transform: scaleX`).
- Брейкпоінти: 560 / 640 / 720 / 768 / 860 / 920 (за потреби компонентів).

### Кнопки

- **Первинна:** заливка `--accent`, текст білий, пігулка, зі стрілкою `→`, яка зсувається на ховері; ховер — `--accent-press`.
- **Вторинна:** прозора, рамка `--line-strong` → `--ink` на ховері.
- **Моно-посилання:** текст акцентом, моно, зі стрілкою (вторинні дії).

### Анімація

- Поява секцій: `opacity` + `translateY(12px)`, easing `cubic-bezier(.22,1,.36,1)`, стаггер 55 мс між елементами групи (`[data-stagger]`).
- Живий візуал героя: скан-лінія апертури (4.6 с цикл), «осідання» барів сигналу, мікрозсув шуму, пульсація beta-індикаторів.
- Ховери: підйом/акцент рамки карток із появою кутового «тіка», обертання квадрата в лого, зсув стрілок.
- `prefers-reduced-motion: reduce` вимикає всі цикли та переходи; скан-лінія ховається, бари статичні.

### Іконки

Ніяких емодзі. Лінійна inline-SVG стрілка (`stroke: currentColor`, `stroke-width: 2`),
квадрат-маркер у лого, службові крапки — усе інше передають номери та моно-мітки.

### Сигнатурний візуал (перший екран) — «виділення сигналу з шуму»

Горизонтальна панель під текстом героя (`.viz`, висота 300 / 230px моб.),
приклеєна до блоку потоку знизу (спільний радіус). Зліва направо:

1. **Шум** — поле дрібного фрактального «зерна» (SVG `feTurbulence`), маскою згасає до центру; моно-мітка `ШУМ`.
2. **Фільтр за темами** — вертикальна апертура акцентного кольору з м'яким світінням; по ній періодично проходить скан-лінія; моно-мітка `ФІЛЬТР ЗА ТЕМАМИ`.
3. **Сигнал** — 5 чистих вирівняних горизонтальних барів (частина акцентні), що «осідають» у ритмі скану; тонка сітка проступає лише праворуч; моно-мітка `СИГНАЛ`.

Під панеллю — блок потоку з 4 вузлів (`01 Джерела · 02 Фільтр за темами ·
03 AI-обробка · 04 Ви`); останній вузол — акцентна заливка. На вузьких
екранах вузли стають вертикальним стовпцем.

Усе поле візуалу декоративне (`aria-hidden`), сама панель має
`role="img"` з описовим `aria-label`.

### Повторювані компоненти

- **Сітка-реєстр** (`.grid` + `.cell`): картки без проміжків, розділені 1px лінією спільного тла; номер моно зверху, `h3`, опис; кутовий «тік» на ховері.
- **Рейка процесу** (`.rail`): 6 однакових кроків із горизонтальною з'єднувальною лінією позаду (десктоп) / вертикальний стек (моб.).
- **Ключова теза** (`.callout`): темний блок (`--ink`), приклеєний до низу сітки рішення; моно-надрядок `КЛЮЧОВА ІДЕЯ`.
- **Статус-панель**: темний моно-хедер зі «живим» зеленим індикатором + світле тіло з визначеннями (`dl`) у форматі «моно-ключ / значення».
- **Рамка скриншота**: моно «breadcrumb» у шапці (`[Назва] / теми / нова`), поле-плейсхолдер із назвою файлу та розміром.
- **Форма**: моно-хедер `ЗАЯВКА НА ПІДКЛЮЧЕННЯ`; вибір типу — сегментований контрол (3 з'єднані сегменти); екран успіху з моно-міткою `ЗАЯВКУ ПРИЙНЯТО`.
- **Футер**: повністю темний; місія великим кеглем; моно-навігація та мета.

---

## 3. Структура сторінки (порядок блоків)

1. Верхній рядок-анонс (1 редагований рядок)
2. Хедер / навігація
3. Перший екран (Hero)
4. Проблема
5. Рішення
6. Як працює зараз (beta)
7. Коротке відео
8. Скриншоти продукту
9. Для кого
10. Загальне бачення
11. Статус запуску (анонс + чесний beta-статус, редагований блок)
12. Форма заявки
13. Фінальний CTA
14. Футер

---

## 4. Блоки: фінальні тексти

### 4.1. Верхній рядок-анонс

Тонка смуга на всю ширину, тло `--accent`, текст `--accent-ink`, один рядок,
по кліку — скрол до форми. Не закривається (щоб не ускладнювати).

> Публічна beta — відкриваємо доступ першим користувачам. **Залишити заявку →**

*Маркер у коді:* `<!-- АНОНС: редагується цей рядок -->`

### 4.2. Хедер / навігація

- Ліворуч: словесний знак `[Назва]`.
- Праворуч (десктоп): якірні посилання — `Проблема` · `Як працює` · `Для кого` · `Beta` — і первинна кнопка **«Отримати beta-доступ»**.
- Мобільний: лише знак + кнопка (без бургер-меню — сторінка коротка).
- Хедер `position: sticky`, напівпрозоре тло з `backdrop-filter: blur`.

### 4.3. Перший екран (Hero)

- **Eyebrow (моно):** СИСТЕМА МОНІТОРИНГУ ПРОФЕСІЙНОЇ ІНФОРМАЦІЇ
- **H1:** Ви визначаєте, що важливо. Система стежить за цим замість вас.
- **Підзаголовок:** Задайте теми, підключіть джерела — і отримуйте лише релевантні матеріали. Без ручного перегляду десятків каналів, сайтів і медіа щодня.
- **Первинний CTA:** Отримати beta-доступ *(скрол до форми)*
- **Вторинний CTA:** Подивитись, як це працює *(скрол до відео)*
- **Рядок довіри під кнопками (моно, дрібний):** Робочий продукт · Публічна beta · Підключення за участю команди на час тестування
- **Візуал:** схема потоку (розділ 2).

### 4.4. Проблема

- **Eyebrow:** ПРОБЛЕМА
- **H2:** Інформації забагато, часу — обмаль
- **Лід:** Щоб залишатися в курсі, доводиться вручну переглядати десятки Telegram-каналів, сайтів і розсилок. Більшість матеріалів не потрібні, важливе легко пропустити, а сам моніторинг забирає години щотижня.
- **Три картки:**
  1. **Забагато джерел.** Потрібне розкидане по каналах, сайтах, чатах і розсилках.
  2. **Забагато шуму.** Корисні матеріали губляться серед реклами, повторів і випадкових новин.
  3. **Забагато ручної роботи.** Регулярний перегляд джерел не масштабується і з'їдає час.

### 4.5. Рішення

- **Eyebrow:** РІШЕННЯ
- **H2:** Інформаційний потік, налаштований під вас
- **Лід:** Ви один раз визначаєте, що для вас важливо. Далі система робить решту.
- **Чотири кроки (пронумеровані):**
  1. **Ви задаєте теми.** Формулюєте, що саме хочете відстежувати.
  2. **Підключаєте джерела.** Канали й медіа, за якими треба стежити.
  3. **Система фільтрує та обробляє.** Збирає матеріали, визначає релевантність, прибирає зайве.
  4. **Ви отримуєте результат.** Тільки те, що відповідає вашому запиту — коротко й по суті.
- **Ключова теза (виділений блок, тло `--accent-soft`):**
  > Не ви підлаштовуєтесь під готовий інформаційний потік — потік підлаштовується під вас або вашу команду.

### 4.6. Як працює зараз (beta)

- **Eyebrow:** ПОТОЧНА BETA-ВЕРСІЯ
- **H2:** Що система вміє вже зараз
- **Лід:** Показуємо реальний поточний стан — без функцій, яких ще немає.
- **Пайплайн** (горизонтальні кроки на десктопі, вертикальні на мобільному):
  `Telegram-джерела` → `Збір публікацій` → `Фільтрація за вашими темами` → `AI-обробка` → `Короткий результат` → `Доставка в Telegram`
- **Примітка під пайплайном:** На час beta підключення джерел і тем відбувається за участю нашої команди. Далі це стане повністю самостійним у інтерфейсі.

### 4.7. Коротке відео

- **Eyebrow:** ДЕМО
- **H2:** Подивіться, як це працює
- **Підзаголовок:** Коротке відео: створення теми → робота системи → відібрана інформація → результат.
- **Плейсхолдер:** рамка 16:9, тло `--surface`, рамка `--line`, кнопка «play» по центру, підпис під нею — «Відео ~20 секунд».
- *Маркер:* `<!-- ВІДЕО: замінити .video-placeholder на <video> або <iframe>; постер — assets/video-poster.jpg -->`

### 4.8. Скриншоти продукту

- **Eyebrow:** ІНТЕРФЕЙС
- **H2:** Реальний продукт, не концепт
- **Підзаголовок:** Кілька екранів поточної beta-версії.
- **Чотири слоти** (сітка 2×2 десктоп, 1 колонка моб.), кожен у «браузерній» рамці з підписом:
  1. Створення теми моніторингу
  2. Стрічка відібраних матеріалів
  3. Картка матеріалу з коротким викладом
  4. Підключені джерела
- **Підпис під блоком:** Зображення з робочої beta-версії. Інтерфейс продовжує вдосконалюватися.
- *Маркери:* `<!-- СКРИНШОТ 1: assets/screen-1.png (рекоменд. 1600×1000) -->` … `СКРИНШОТ 4`
- До запуску прописати змістовний `alt` кожному зображенню.

### 4.9. Для кого

- **Eyebrow:** СЦЕНАРІЇ
- **H2:** Для приватних користувачів, спеціалістів і команд
- **Три картки:**

  **Приватний користувач**
  Власні інтереси, навчання, інвестиції, свій бізнес, персональні
  інформаційні задачі.

  **Спеціаліст**
  Постійний моніторинг своєї професійної сфери: важливі події та зміни,
  галузеві новини, професійні матеріали. Залишайтеся в контексті без
  постійного ручного перегляду джерел.

  **Компанія / команда**
  Конкуренти, ринок, законодавчі зміни, маркетинг, HR, технології та інші
  корпоративні напрями.

- **Примітка під картками:** Це приклади, а не обмежений перелік. Систему можна налаштувати під будь-яку тему, за якою існують публічні джерела.

### 4.10. Загальне бачення

- **Eyebrow:** БАЧЕННЯ
- **H2:** Поточна версія — перший крок
- **Текст (3 речення):** Ми будуємо не тематичний дайджест і не окремий Telegram-бот. Мета — універсальна система персонального, професійного та корпоративного моніторингу інформації, що працює під конкретні завдання кожного користувача. Поточна beta — фундамент цієї системи.

### 4.11. Статус запуску *(редагований блок)*

Помітна панель: тло `--accent-soft`, рамка `--accent` 1px, радіус 12px.
Об'єднує анонс запуску (п.8 брифу) і чесний beta-статус (п.9 брифу).
Увесь текст усередині — в одному коментованому блоці, щоб редагувати без
дотику до решти сайту.

- **Eyebrow:** СТАТУС
- **H2:** Ми у публічній beta
- **Статус:** Продукт працює і зараз готується до відкритого beta-тестування.
- **Що зараз:** Набираємо перших користувачів для підключення. Доступ відкриваємо поступово.
- **Чесно:** Система продовжує вдосконалюватися — насамперед щодо якості та стабільності фільтрації. Це робочий продукт, а не прототип: він уже виконує свою основну задачу.
- **CTA:** Залишити заявку на підключення
- *Маркер:* `<!-- СТАТУС ЗАПУСКУ: увесь блок вільно редагується -->`

### 4.12. Форма заявки

- **Eyebrow:** ЗАЯВКА
- **H2:** Залишити заявку на beta-доступ
- **Підзаголовок:** Кілька полів — щоб зрозуміти ваш сценарій і відкрити доступ.

**Крок 1 — тип підключення** (три кнопки-пігулки, реалізовані як
`radio`-інпути зі стилізованими лейблами; обов'язково):

`Для себе` · `Я спеціаліст` · `Для компанії`

**Поля, спільні для всіх типів:**

| Поле | Тип | Обов'язкове | Плейсхолдер |
|---|---|---|---|
| Ім'я | text | так | — |
| Пошта або Telegram | text | так | `you@email.com або @username` |
| Що хочете відстежувати? | textarea | так | `Теми, ринки, напрями, ключові питання…` |

**Додаткові поля за типом:**

- **Для себе:**
  - Сфера інтересу — text, необов'язкове
  - Які джерела моніторите зараз? — textarea, необов'язкове
- **Я спеціаліст:**
  - Професія / сфера — text, необов'язкове
  - Які джерела моніторите зараз? — textarea, необов'язкове
- **Для компанії:**
  - Компанія — text, необов'язкове
  - Посада / роль — text, необов'язкове
  - Напрями моніторингу — textarea, необов'язкове
  - Приблизна кількість користувачів — select: `1–3` / `4–10` / `11–50` / `50+`
  - Хто користуватиметься системою — text, необов'язкове

**Кнопка надсилання:** Надіслати заявку

**Дрібний текст під кнопкою:** Відповімо на вказаний контакт. Дані
використовуємо лише для підключення до beta.

**Екран успіху** (замінює форму після надсилання, **лише в робочому режимі**):
> **Дякуємо! Заявку отримано.**
> Ми зв'яжемося з вами найближчим часом на вказаний контакт.

**Поведінка (JS):**
- Вибір типу перемикає видимість груп додаткових полів; неактивні групи отримують `disabled`, тому їхні поля не потрапляють у `FormData`.
- Валідація обов'язкових полів (`required`), підсвічування помилок рамкою.
- Атрибут `data-endpoint` на `<form>` — єдиний перемикач між демо- і робочим режимом (див. нижче).

*Маркер:* `<!-- ФОРМА: задати endpoint у data-endpoint на <form> (Formspree / Getform / власний API). Порожній = демо-режим. -->`

#### Два режими форми

Поточний стан сайту — **демонстраційний**: він використовується як приклад
у портфоліо, тому не має вдавати, що приймає заявки.

| | Демо-режим (`data-endpoint=""`) | Робочий режим (endpoint заданий) |
|---|---|---|
| Попередження у формі | показується жовта плашка «Демонстраційний проєкт — форма не підключена…» | приховане |
| Підпис під кнопкою | «Демо-режим: дані не надсилаються і не зберігаються.» | «Відповімо на вказаний контакт…» |
| Після надсилання | панель **«Форма ще не підключена»** + кнопка «Повернутись до форми» | екран «Дякуємо! Заявку отримано» |
| Мережевий запит | **не відбувається жодного** | `fetch` `POST` `FormData` на endpoint |
| Дані користувача | не надсилаються і не зберігаються | надходять на endpoint |

Перемикання автоматичне: досить задати `data-endpoint` — плашка й панель
демо-режиму зникають самі, нічого видаляти не потрібно.

**Панель демо-режиму:**
> **ДЕМО-РЕЖИМ**
> **Форма ще не підключена**
> Це демонстраційний проєкт із портфоліо. Ваші дані нікуди не надіслані
> й не збережені — заявки поки не приймаються. У робочій версії форма
> надсилає заявку на підключений endpoint.
> `[Повернутись до форми]`

**Помилка надсилання (робочий режим):** «Не вдалося надіслати. Спробуйте
ще раз або напишіть на `[контакт]`».

### 4.13. Фінальний CTA

- **H2:** Готові спробувати?
- **Підзаголовок:** Приєднуйтесь до перших користувачів beta.
- **CTA:** Отримати beta-доступ

### 4.14. Футер

- Словесний знак `[Назва]`.
- Місія одним реченням: *Потрібна інформація має знаходити вас сама — а не навпаки.*
- Мінімальні посилання: `Проблема` · `Як працює` · `Для кого` · `Заявка`.
- Контакт: `[контакт — заповнюється перед запуском]` *(явний плейсхолдер — навмисно не схожий на справжній контакт)*.
- `© 2026 [Назва]`
- **Демо-примітка** (маркер `<!-- ДЕМО-ПРИМІТКА -->`, видаляється при реальному запуску):
  *Демонстраційний проєкт із портфоліо. Назва, контакти, скриншоти та відео — умовні; форма заявки не підключена й даних не збирає.*

---

## 5. `<head>` / SEO / шеринг

```html
<html lang="uk">
<title>[Назва] — система моніторингу професійної інформації</title>
<meta name="description" content="Персональна та корпоративна система моніторингу інформації. Задайте теми, підключіть джерела — отримуйте лише релевантне. Публічна beta.">
<meta property="og:title" content="[Назва] — система моніторингу професійної інформації">
<meta property="og:description" content="Ви визначаєте, що важливо. Система стежить за цим замість вас. Публічна beta.">
<meta property="og:image" content="assets/og-image.png">
<meta property="og:type" content="website">
<meta name="twitter:card" content="summary_large_image">
```

- Favicon — плейсхолдер `assets/favicon.svg`.
- Слот під аналітику (Plausible / GA) — коментар перед `</head>`.

---

## 6. Доступність і продуктивність

- Семантичні лендмарки (`header`, `main`, `section`, `footer`), один `h1`, логічний порядок заголовків.
- `label` прив'язані до полів; видимий `:focus-visible`; перемикач типу — справжні `radio` (керується клавіатурою).
- Контраст тексту — не нижче AA.
- **Зовнішніх залежностей немає взагалі.** Шрифти self-hosted у `assets/fonts/`,
  JS-бібліотек немає. Сторінка не робить жодного запиту до сторонніх доменів,
  тому IP відвідувача нікуди не передається (це прибирає ризик за GDPR, який
  створює завантаження шрифтів із `fonts.gstatic.com`).
- Шрифти: 24 файли `.woff2` — 2 гарнітури × 3 накреслення (400/500/600) ×
  4 підмножини (`latin`, `latin-ext`, `cyrillic`, `cyrillic-ext`), ~511 КБ на диску.
  Завдяки `unicode-range` браузер тягне лише потрібні: на українській сторінці
  це 12 файлів. Повна вага сторінки зі шрифтами — близько 195 КБ.
- `preload` для двох критичних накреслень (Sans 400 і 600, кирилиця) — прибирає
  стрибок шрифту в заголовку.
- Ліцензія шрифтів: OFL 1.1, копія в `assets/fonts/OFL.txt`.
- Скриншоти — `loading="lazy"`, задані `width`/`height`.
- `prefers-reduced-motion` вимикає анімації.
- Цільова вага сторінки без медіа — < 60 КБ.

---

## 7. Дії перед запуском (checklist)

Продубльовано коментарем на початку `index.html`.

1. `[Назва]` → реальна назва: словесний знак, `<title>`, OG-теги, футер.
2. Текст верхнього рядка-анонсу (`<!-- АНОНС -->`).
3. Блок «Статус запуску» (`<!-- СТАТУС ЗАПУСКУ -->`).
4. Відео: вставити `<video>`/`<iframe>` + постер.
5. Чотири скриншоти + змістовні `alt`.
6. Endpoint форми в `data-endpoint` *(автоматично вимикає демо-режим)*.
7. Контакт у футері та в тексті помилки форми.
8. За потреби — сніпет аналітики (бажано cookieless, напр. Plausible).
9. Видалити демо-примітку у футері (`<!-- ДЕМО-ПРИМІТКА -->`).
10. Перегенерувати `assets/og-image.png` після заміни `[Назва]`
    (див. розділ 8).
11. **Абсолютні URL** в `og:url` і `og:image` — оновити домен при переїзді
    з GitHub Pages (маркер `<!-- URL -->`). Відносні шляхи соцмережі не
    резолвлять, і превью не збереться.
12. Сторінка `privacy.html` + посилання у футері та під формою — форма
    збирає персональні дані (ім'я, контакт, компанія, посада, робочі
    задачі), тож потрібні контролер, підстава обробки, строк зберігання
    і права суб'єкта. **Обов'язково до підключення endpoint.**
13. Honeypot-поле проти ботів (endpoint видно у публічному коді).

Уже зроблено: self-hosting шрифтів, `favicon.svg`, `og-image.png`,
демо-режим форми.

### Допоміжні скрипти (`scripts/`)

| Файл | Призначення |
|---|---|
| `fetch-fonts.py` | Перезавантажує підмножини IBM Plex і генерує `@font-face`. Потрібен лише при зміні набору накреслень. |
| `og-server.py` | Локальний сервер на `127.0.0.1:8790`: віддає проєкт і приймає готовий PNG. |
| `og-generator.html` | Малює `og-image.png` (1200×630) на canvas тими самими шрифтами. Запустити `og-server.py` і відкрити `http://127.0.0.1:8790/scripts/og-generator.html` — файл перезапишеться. |

Це інструменти збірки; на роботу сайту вони не впливають.

---

## 8. Заміна назви — одне місце

У `index.html` назва винесена в CSS-незалежний текстовий вузол і згадана
лише в маркованих місцях (усі позначені `<!-- [Назва] -->`):
словесний знак у хедері, `<title>`, OG-теги, футер (знак + копірайт).
Заміна — пошук-заміна по рядку `[Назва]`.

---

## 9. Мапа секцій → вимоги брифу

| Вимога брифу | Секція специфікації |
|---|---|
| 1. Перший екран | 4.3 |
| 2. Проблема | 4.4 |
| 3. Рішення (+ ключова ідея) | 4.5 |
| 4. Як працює зараз | 4.6 |
| 5. Скриншоти | 4.8 |
| 6. Коротке відео | 4.7 |
| 7. Для кого (3 сценарії) | 4.9 |
| 8. Блок анонсу / статус запуску | 4.1 + 4.11 |
| 9. Beta-статус (чесно) | 4.11 |
| 10. Загальне бачення | 4.10 |
| 11. Форма заявки (адаптивна) | 4.12 |
| Місія та позиціонування | 1 |
| Візуальні вимоги | 2 |
